# Belsley (1991). "Conditioning Diagnostics"
# The Consumption Function (pp. 149-154)
consumption<-read.table(file="consumption.dat",header=TRUE)
consumption<-data.frame(consumption)
attach(consumption)
